export const metadata = {
  title: "Login | CareerOS AI",
  description: "Sign in to CareerOS AI",
};

export default function LoginLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
